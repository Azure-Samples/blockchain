# Simple Private IPFS deployment Virtual Machine

The purpose of this ARM template is **simple private IPFS deployment via Virtual Machine** inserting a few parameters to help configure the network.

## The Template
